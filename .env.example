PORT=8787
VITE_API_URL=http://localhost:8787
CORS_ORIGIN=http://localhost:5173
# Leave blank for the disposable local store; required in production.
MONGODB_URI=
MONGODB_DATABASE=mealwise
# auto: use OpenAI when a key is present, otherwise the limited local parser.
# model: require OpenAI; local: offline parser only. All offers are mock data.
AGENT_MODE=auto
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
